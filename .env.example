# Kopiere diese Datei nach .env und passe die Werte an.

# Zufälliger geheimer Schlüssel für JWT-Tokens (mind. 32 Zeichen)
# Generieren z.B. mit: openssl rand -hex 32
JWT_SECRET=aendern-auf-einen-langen-zufaelligen-string

# Port für das Backend (API)
PORT=8080

# Port für das Frontend (Web-UI)
UI_PORT=8090

# Paperless NGX Integration (optional)
# Wenn gesetzt, werden PDFs bei Archivierung automatisch nach Paperless NGX hochgeladen.
# PAPERLESS_NGX_URL=http://paperless:8000
# PAPERLESS_NGX_TOKEN=dein-api-token-aus-paperless
